package space.bbkr.impatience;

import net.minecraft.block.BlockStairs;
import net.minecraft.block.state.IBlockState;

public class BlockImpatientStairs extends BlockStairs {

    protected BlockImpatientStairs(IBlockState state, Builder builder) {
        super(state, builder);
    }
}
